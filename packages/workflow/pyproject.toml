[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "smorx-workflow"
version = "0.1.0"
description = "Phase 12 Workflow: E2E orchestration, state replay, failure injection, demo reliability, performance."
requires-python = ">=3.11"
dependencies = [
    "smorx-contracts",
    "smorx-behavior",
    "smorx-runtime",
    "smorx-tools",
    "smorx-precode",
    "smorx-develop",
    "smorx-certification",
]
classifiers = [
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM", "ASYNC", "RUF"]
ignore = ["E501"]

[tool.ruff.format]
quote-style = "double"

[tool.mypy]
python_version = "3.11"
strict = true
plugins = ["pydantic.mypy"]
